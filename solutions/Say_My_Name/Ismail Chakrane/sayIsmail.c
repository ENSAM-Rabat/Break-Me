#include <stdio.h>
#include <stdlib.h>

double list[] = {17200164810932.71, 6 };
char *node = (char *)list;
int main(){
	
list[1] && ( list[0] /= 2,putchar(main(--list[1], (char **)++node)));
return *--node;
}